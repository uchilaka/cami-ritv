---
id: 2026-09-20-linear-lar-328 # <date>-<origin>-<ref>, also the filename
title: LAR-328 — link TODO comments to tickets
origin: linear # linear | github-pr | article | transcript | email | other
url: https://linear.app/larcity/issue/LAR-328
retrieved: 2026-09-20
---

<!--
  Copy to ../sources/external/<id>.md — for NON-repository sources only.

  Repository sources are never copied: git holds them immutably, so a page cites
  `path:line` plus a commit instead. Use this file when the source would
  otherwise be lost — a ticket that may be edited, a page that may 404, a
  conversation that exists nowhere else.

  Record the source as retrieved. Do not summarise it here and do not correct it;
  interpretation belongs on the pages that cite this file. If the original later
  changes, add a new source file rather than editing this one — pages already cite
  this id.

  REDACTION IS THE ONE EXCEPTION TO "VERBATIM". A ticket, transcript or email is
  not vetted the way this repository is: it can carry a token, a password or a
  connection string even when the code it describes is clean. Read the content
  before pasting it, replace every credential VALUE with <redacted> — keeping the
  key name, which is usually the part a page needs — and note the redaction below
  so a later reader knows the omission was deliberate rather than a gap in the
  source. wiki/CLAUDE.md forbids credential values anywhere in the wiki, and this
  file is committed like any other.

  A redacted secret is still a leaked secret if it was ever real: tell the user
  what you found and where, so it can be rotated. Never paste first and clean up
  afterwards — the value lands in git history at the first commit.
-->

Verbatim content, apart from redacted credential values (see above), or enough of
it that a page citing this source can be checked without the original.

Note what the source does *not* say — an ingest's most useful output is often a
question, and a reader cannot tell a gap from an oversight unless you say so.
