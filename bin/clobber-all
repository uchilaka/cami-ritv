#!/usr/bin/env zsh

# Calculate the application root directory
APP_ROOT_DIR="$(dirname "${0:A}" | xargs dirname)"

"$APP_ROOT_DIR/bin/rails" dartsass:clobber tailwindcss:clobber assets:clobber &&\
  "$APP_ROOT_DIR/bin/rails" dartsass:build tailwindcss:build
