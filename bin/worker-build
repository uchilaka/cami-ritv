#!/usr/bin/env bash
# Exit on error
set -o errexit

# Calculate script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Decrypt secrets
"$SCRIPT_DIR/unlock-repo"

# Update bundler
gem install bundler -v '~> 2.6'

# Install gems
bundle install
