#!/usr/bin/env zsh

# Intentionally skips the tunnel service, as it is only needed
# when testing features like SSO or payment integrations
docker compose --profile=batteries-included up -d &&\
  docker compose --profile=batteries-included logs --follow
