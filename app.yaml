# CONSIDER UPDATING THE TEMPLATE INSTEAD OF EDITING THIS FILE DIRECTLY.
# It is generated from config/apps/production.yaml.erb.
#
# You can also request the configured blueprint for this application by
# running the `lx-cli blueprint:get` command.
#
# This file is used to configure the remote DigitalOcean application.
# It is processed by ERB before being used, allowing you to include
# dynamic content.
# -------------------------------------------------------------------------
# Automatically generated at 2026-03-20T20:53:45-04:00
# -------------------------------------------------------------------------

alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED
databases:
  - cluster_name: db-postgresql-production-larcity
    engine: PG
    name: db-postgresql-production-larcity
    production: true
features:
  - buildpack-stack=ubuntu-22
ingress:
  rules:
    - component:
        name: cami-production-web
      match:
        authority:
          exact: ""
        path:
          prefix: /
name: larcity-production-app
region: nyc
services:
  - build_command: ./bin/build
    environment_slug: ruby-on-rails
    envs:
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${db-postgresql-production-larcity.DATABASE_URL}
    github:
      branch: releases/production
      deploy_on_push: true
      repo: uchilaka/cami-ritv
    http_port: 16006
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb
    name: cami-production-web
    run_command: ./bin/pre-deploy && rails server -p $PORT -e ${RAILS_ENV:-production}
    source_dir: /
vpc:
  id: 5793d440-fc51-4800-805d-02be90495f20
workers:
  - environment_slug: ruby-on-rails
    envs:
      - key: DATABASE_URL
        scope: RUN_AND_BUILD_TIME
        value: ${production-database.DATABASE_URL}
    github:
      branch: releases/production
      deploy_on_push: true
      repo: uchilaka/cami-ritv
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb
    name: cami-production-worker
    run_command: rails solid_queue:start
    source_dir: /
